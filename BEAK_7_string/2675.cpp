/////////////////////////////////////////////////////////////////////////////////////////////
/////////////////////////////////////////////////////////////////////////////////////////////

#include<iostream>
#include <cstdio>
#include <cstring>


using namespace std;
//int map[501][501];
int main(int argc, char** argv)
{
	int M,N,T,rp;
	char buf[21];
	memset(buf,0,sizeof(buf));
	scanf("%d",&T);
	for(int tc=0 ; tc<T ; tc++) {
		scanf("%d %s",&rp,&buf);
		int len = strlen(buf);
		for(int i=0 ; i<len ; i++) {
			for(int j=0 ; j<rp ; j++) {
				printf("%c",buf[i]);
			}
		}
		puts("");
		
	}
	
	
	/////////////////////////////////////////////////////////////////////////////////////////////
	/**/
	/////////////////////////////////////////////////////////////////////////////////////////////


	
	return 0;//
}
